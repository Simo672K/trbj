import 'package:flutter/material.dart';

class PayrollStateCard extends StatelessWidget {
  const PayrollStateCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
